# CORE_TOUCHES.md — 上游文件改动登记

每登记一条：文件、原因、「上游 PR 候选？」标记。每次同步仪式（ADR 0001）逐条重放验证；被上游接受的改动在下一次 sync 后划掉。

| 文件 | 改动 | 原因 | 上游 PR 候选？ | 登记批次 |
|---|---|---|---|---|
| `AGENTS.md` | 文末追加「daypaw-pro (fork layer)」节（issue tracker / triage labels / domain docs 指引） | pi 项目指引须随 AGENTS.md 自动加载；上游段落保持原样、上游所有 | 否（fork 私有流程） | A（fork 导入） |
| `tsconfig.base.json` | paths 新增 `@daypaw/*`、`@daypaw/*/invariant` 两个通配组与 `@daypaw/web-app/startup` 子路径映射（daypaw 新 family 的源解析）；#59 起再增 `@daypaw/*/client` → `packages/daypaw/*/src/client` 一条（fork UI 插件互拉 SlotMap/Context 类型合并的源面解析）；#57 起再增 `@daypaw/*/types` → `packages/daypaw/*/src/types.ts` 一条（fork 纯类型出口的源面解析） | 新 family 纯新增的共享配置加性例外（ADR 0001/ADR 0006；加包清单 §2 预期项）；startup 子路径同上游 `dsh-web-app/startup` 显式映射先例；/client 子路径同上游 `dsh-client-*/client` 通配先例；/types 子路径同上游 `dsh-plan-mode/types` 先例 | 可提（上游无 daypaw，但通配组模式可参考） | C（走骨）/F（产品壳脚手架）/#59（设置单面页）/#57（任务进度） |
| `tsconfig.host.json` | references 新增 `packages/daypaw/{store,engine,sdk,cli,web-app}` 五行（host 聚合）；#57 起追加 `packages/daypaw/approval-history` 一行 | 同上；项目引用无通配形式，只能逐包登记 | 可提（同上） | C（走骨）/F（产品壳脚手架）/#57（任务进度） |
| `tsconfig.client.json` | references 新增 `apps/daypaw-web` 与 `packages/daypaw/ui-inbox` 两行；include 新增 `packages/daypaw/ui-inbox/src/css-modules.d.ts` 一条（client 聚合的 fork 条目）；#59 起 references 追加 `packages/daypaw/ui-settings` 一行、include 追加 `packages/daypaw/ui-settings/src/css-modules.d.ts` 一条；#56 起 references 追加 `packages/daypaw/ui-tasks` 一行、include 追加 `packages/daypaw/ui-tasks/src/css-modules.d.ts` 一条；#60 起 references 追加 `packages/daypaw/ui-agents` 一行、include 追加 `packages/daypaw/ui-agents/src/css-modules.d.ts` 一条；#61 起 references 追加 `packages/daypaw/ui-brand` 一行（纯 token 层包，无 css-modules 条目） | 同上；项目引用无通配形式，只能逐包登记；css-modules 条目沿用 `packages/extensions/ui-cordis` 的聚合 include 先例 | 否（fork 私有包） | F（产品壳脚手架）/#55（收件箱骨架）/#59（设置单面页）/#56（任务对话）/#60（agent 目录）/#61（壳品牌主题） |
| `scripts/check-workspace-constraints.ts` | release-member 目录正则排除 `packages/daypaw/` 组与 `apps/daypaw-*`（负向前瞻）；另设 `publishableDaypawPackages`（cli/sdk）：豁免 private 要求、改验 `publishConfig.access: public` 与 fork 仓库 repository 字段，并豁免其 peerDependencies 的 workspace: 协议要求 | 上游 npm-public 化后 `packages/*/*`、`apps/*` 一律要求可发布，与 ADR 0001「@daypaw 独立 0.x private」相抵；ADR 0011 核准 cli/sdk 两包可发布、peer 指向上游 npm 发布 | 可提（上游无 daypaw；规则形状属上游发布流程） | C（走骨）/D（客户交付）/F（产品壳脚手架） |
| `scripts/package-invariants.ts` | `@daypaw/sdk` 豁免 dsh-invariants peer 必须为 `workspace:^` 一条（dev 仍验） | ADR 0011：sdk 发布时该 peer 是面向消费方的 npm range，workspace 接线留在 devDependencies | 否（fork 发布形态无上游对应物） | D（客户交付） |
| `knip.json` | workspaces 新增 `packages/daypaw/cli`（entry/project 同规约 + `ignoreDependencies: @deepseek-ai/.+`；#62 起另豁免 `@daypaw/web-app`——闭包 manifest 行，非代码引用）、`apps/daypaw-web`（closure manifest 豁免；#56 起 entry/project 扩到 `tests/**/*.snapshot.ts`/`tests/**/*.ts`，同 apps/web 的 snapshot 道形状）与 `packages/daypaw/web-app`（closure manifest 豁免；#55 起另豁免 roster 行命名的 `@daypaw/ui-inbox`，#59 起再豁免 `@daypaw/ui-settings`，#56 起再豁免 `@daypaw/ui-tasks`，#60 起再豁免 `@daypaw/ui-agents`，#57 起再豁免 `@daypaw/approval-history`，#61 起再豁免 `@daypaw/ui-brand`）、`packages/daypaw/ui-inbox` 与 `packages/daypaw/ui-settings`（entry/project 同上游含 .tsx spec 的 UI 包规约；#56 起同形新增 `packages/daypaw/ui-tasks`，#60 起同形新增 `packages/daypaw/ui-agents`，#61 起同形新增 `packages/daypaw/ui-brand`（纯 token 层包）；#57 起同形新增 `packages/daypaw/approval-history`，host 包形、entry/project 同规约） | cli 与 web-app 是闭包 manifest 包，dependencies 列表刻意不被代码引用（同 apps/cli、packages/bundle/web-app 先例）；daypaw-web 同 apps/web 形状减 e2e 道；fork UI 包的 spec 为 .tsx，默认 `packages/*/*` 块只覆盖 .ts | 否（fork 私有包） | D（客户交付）/F（产品壳脚手架）/#55（收件箱骨架）/#59（设置单面页）/#56（任务对话）/#60（agent 目录）/#57（任务进度）/#62（直起壳交付）/#61（壳品牌主题） |
| `packages/client/connection/src/client/fixture.ts` | rpc `call` switch 新增 `durable/listDefinitions` 端点（两条 fixture 定义：一条带 display、一条不带）；#57 起再增 `durable/listRuns`/`durable/runLineage`/`durable/journalTimeline`/`durable/rerun` 四端点应答（模块级可变运行表：fx-alpha 带 session 孪生的 running agent run、fx-run-invoice-audit 失败顶层 agent run 含一个失败子 run、fx-run-release-digest 带 output_json 与三步 journal 的 done workflow run；rerun 追加的 attempt 行对下一次轮询可见），并把 approvalHistory 折叠进 projectionValuesOf/projectionFramesOf、fx-alpha 日志追加两条 approval asked/decided 对；#58 起常驻审批改带 callId 并与 fx-alpha 日志新增的 turn-75 开放 tool/call 配对（后续修复：todo_write 样本改乘同一开放 turn 的已完成 step 0，避免后缀 turn/start 退役 standing plan 致上游 todo 面空置）、常驻 question 迁至 fx-gamma（避免 question 徽章在 run 孪生 session 上遮蔽 approval 徽章）、fx-gamma 的 5s running 翻转改为 `FixtureOptions.flipGammaRunning` 显式开启（环境翻转会与任何把 sessions 列表采进 golden 的车道竞态，唯 fixture spec 钉它）；#67 起定义表补齐 `inputKind` 并前置 starter-assistant（starter `{ task }` text 形状），再增 `durable/startRun` 应答（注册表按名+版本解析（缺省版本拒绝歧义并列名候选，引擎平行）、text 形状裸字符串收拢为 `{ task }`、runId 存在即 start-or-attach 直答、否则追加 running 行并创建 sessionId≡runId 的 session 孪生后以内部 prompt 驱动首轮——首条 user message = 输入的 JSON 序列化，ADR 0010 平行） | daypaw 组装快照车道的免 key 运输：引擎注册表只读视图（#51）与运行 ledger 查询面（#50）经 gateway Remote 通道供给浏览器，fixture 须能应答这些端点（`goals/*` 先例）；fork 专属端点上游无对应物 | 否（fork 私有端点的 fixture 应答） | #60（agent 目录）/#57（任务进度）/#58（审批待办）/#67（壳发起 run 壳侧） |
| `package.json`（root） | scripts 新增 `release:daypaw`（`tsx scripts/release/daypaw.ts`）；`build:web` 扩展为同时构建 `@daypaw/web-frontend`；#56 起新增 `test:web:daypaw`（`npm run build && vitest run --config vitest.web.daypaw.config.ts`，含 `:built`/`:refresh` 变体，同 `test:web` 先例） | ADR 0011 发布工程面入口；fork 前端 dist 随全量 build 产出（spec 第 5 章 §4）；fork 组装 web 快照车道的脚本入口 | 否（fork 私有流程） | D（客户交付）/F（产品壳脚手架）/#56（任务对话） |
| `.gitignore` | 新增 `dist-daypaw/`（release 暂存与 tarball 输出）；新增 `apps/daypaw-web/dist/`（fork 前端构建产物） | 同 `dist-exe/`、`apps/web/dist/` 先例 | 否（fork 私有产物） | D（客户交付）/F（产品壳脚手架） |
| `scripts/doc-budgets.manifest.json` | `AGENTS.md` 词预算 1900 → 1975 | 上游本体已顶满 1900，任何 fork layer 追加都超限；提上限而非持续压缩上游段落 | 否（fork 层增量无上游对应物） | C（走骨） |
| `scripts/translation-pairing.ts` | 翻译范围排除 fork 设计语料（docs/{adr,spec,agents,fork,reports,research}、CONTEXT.md） | fork 设计语料中文优先是图定决策（map #1），双语义务覆盖上游文档 | 否（fork 语料无上游对应物） | C（走骨） |
| `apps/web/tests/built-boot.snapshot.ts` | 常驻交互断言从「Waiting for answer」改为「Waiting for approval」 | #58 把常驻 question 迁至 fx-gamma 后，fx-alpha 的可操作等待是审批；上游烟雾钉的正是该 session 行的可操作等待镜像 | 可提（断言文案随 fixture 状态走，属上游测试自身的适应性修改） | #58（审批待办） |
| `scripts/client-tsconfig.spec.ts` | `clientCssDeclarations()` 的 clientGroups 枚举新增 `'daypaw'` | client 聚合 css-modules 契约测试的 group 枚举须覆盖 `packages/daypaw/*/src/css-modules.d.ts`（ui-inbox 自 #55 起在 include 里，枚举漏了该组） | 可提（枚举随 group 增长是上游机制；daypaw 组属 fork） | #59（设置单面页） |
| `scripts/gen-cordis-catalog.ts` | `ctx.durable` 入 SERVICE_WALK_EXEMPTIONS；EngineDefinition/EngineRunHandle/EngineRunOptions 入 TYPE_LINK_EXEMPTIONS（#53 起 steer wire 输入的 `Json` 类型同入） | fork 服务的文档之家是包 README（上游子系统目录外），签名类型链接随之豁免 | 可提（豁免模式本身是上游机制；具体条目属 fork） | C（走骨）/#53（steer 通道） |
| `package.json`（root） | devDependencies 新增 `zod@^4.4.3` | doc-typecheck 从根级临时项目编译文档代码块，裸 `zod` 导入只能经根 node_modules 解析（pnpm 不提升到根）；daypaw spec/README 的正典类型与示例必须 import zod | 否（上游文档块未导入 zod，无对应需求） | E（defineAgent 编译面） |
| ~~`packages/boot/app-boot/src/profile.ts`（#64）~~ | **已消解（2026-08-28 sync）**：上游重写 heal 面为 `ProfileModuleFallbackOptions` + module-proxy 机制后，daypaw/cli 只调用上游公开的 `healProfilesModuleFallback({installAnchor, home})`，不再需要导出 `ensureSymlink`——fork 副本已随 #62 删除，无上游文件改动 | — | ~~#64~~（2026-08-28 sync 消解） |
| `packages/client/ui-theme/src/theme-settings.ts`（连带 `src/client/settings-store.ts` 改读 `DEFAULT_PREFERENCE`、`tests/{theme,boot-theme,apply,settings-store,host}.client.spec.ts` 默认断言、README(.zh) 默认描述两句、`apps/web/tests/snapshots/settings-chrome/dialog.expected.md` 外观行 pressed 态 golden 刷新） | `DEFAULT_PREFERENCE` 从 `'system'` 改为 `'light'` | #61：spec 05 §7 裁决「亮主题默认」；settings namespace 归属注册不可重叠、插件无 config 钮、种子用户 settings.yaml 会污染与 dsh 共享的文档——ADR 0001 §4 三问皆无解，唯一落点是常量本身。影响面：上游 dsh web 壳的默认偏好同步变 light（TUI 无此插件）；偏好切换面（含跟随系统）不变 | 否（daypaw 产品决策；上游 dev 工具默认 system 是其自身取舍） | #61（壳品牌主题） |
| `scripts/verify-application-entrypoints.ts` | `MANIFEST_BIN_ALLOWLIST` 增 `packages/daypaw/cli`（`daypaw` → `bin.mjs`）；`EXECUTABLE_SOURCE_ALLOWLIST` 增 `packages/daypaw/cli/bin.mjs` | ADR 0011：daypaw CLI 是自含交付（bin 直起、不经 dsh launcher），登记为合法例外 | 否（fork 交付形态） | 2026-08-28 sync |
| `scripts/translation-pairing.manifest.json` | `excluded` 增 fork 单语设计语料（`docs/adr/`、`docs/spec/`、`docs/agents/`、`docs/fork/`、`docs/reports/`、`docs/research/`、`CONTEXT.md`） | fork 设计语料中文优先（map #1 定案），无英文 twin；上游新配对规则（2026-08-28 收紧）要求 zh 侧链接带 `.zh`，语料外目标保持 authored 路径 | 否（fork 语料无上游对应物） | 2026-08-28 sync |
| `vitest.config.ts` | coverage exclude 新增一条 glob `packages/daypaw/ui-*/src/**` | ADR 0007 §1 裁决：fork 的 daypaw ui-* 家族沿用上游 client UI 的 GUI 债豁免姿态（jsdom 组件测试 + 组装 web 车道，不受 per-file 门约束）；host 侧包（engine 等）仍走 100% 门 | 可提（豁免模式本身是上游机制；具体 glob 属 fork 家族） | #71（覆盖率门修复） |
| `scripts/release/families.ts` | `DshFamily.members()` 跳过 fork 自营目录（`packages/daypaw/`、`packages/examples/daypaw-skeleton/`、`apps/daypaw-web/`） | @daypaw/* 包（ADR 0011 客户交付形态）由独立 daypaw release family 发布，不混入 dsh family；patterns 保持上游原样减少 sync 冲突 | 否（fork 包命名架构） | 2026-08-28 sync |
