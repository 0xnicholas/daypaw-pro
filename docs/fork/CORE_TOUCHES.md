# CORE_TOUCHES.md — 上游文件改动登记

每登记一条：文件、原因、「上游 PR 候选？」标记。每次同步仪式（ADR 0001）逐条重放验证；被上游接受的改动在下一次 sync 后划掉。

| 文件 | 改动 | 原因 | 上游 PR 候选？ | 登记批次 |
|---|---|---|---|---|
| `AGENTS.md` | 文末追加「daypaw-pro (fork layer)」节（issue tracker / triage labels / domain docs 指引） | pi 项目指引须随 AGENTS.md 自动加载；上游段落保持原样、上游所有 | 否（fork 私有流程） | A（fork 导入） |
| `tsconfig.base.json` | paths 新增 `@daypaw/*` 通配组与 `@daypaw/web-app/startup` 子路径映射（daypaw 新 family 的源解析；#87 起原 `@daypaw/*/invariant` 子映射随空壳 companion 一并移除）；#59 起再增 `@daypaw/*/client` → `packages/daypaw/*/src/client` 一条（fork UI 插件互拉 SlotMap/Context 类型合并的源面解析）；#57 起再增 `@daypaw/*/types` → `packages/daypaw/*/src/types.ts` 一条（fork 纯类型出口的源面解析） | 新 family 纯新增的共享配置加性例外（ADR 0001/ADR 0006；加包清单 §2 预期项）；startup 子路径同上游 `dsh-web-app/startup` 显式映射先例；/client 子路径同上游 `dsh-client-*/client` 通配先例；/types 子路径同上游 `dsh-plan-mode/types` 先例 | 可提（上游无 daypaw，但通配组模式可参考） | C（走骨）/F（产品壳脚手架）/#59（设置单面页）/#57（任务进度） |
| `tsconfig.host.json` | references 新增 `packages/daypaw/{store,engine,sdk,cli,web-app}` 五行（host 聚合）；#57 起追加 `packages/daypaw/approval-history` 一行 | 同上；项目引用无通配形式，只能逐包登记 | 可提（同上） | C（走骨）/F（产品壳脚手架）/#57（任务进度） |
| `tsconfig.client.json` | references 新增 `apps/daypaw-web` 与 `packages/daypaw/ui-inbox` 两行；include 新增 `packages/daypaw/ui-inbox/src/css-modules.d.ts` 一条（client 聚合的 fork 条目）；#59 起 references 追加 `packages/daypaw/ui-settings` 一行、include 追加 `packages/daypaw/ui-settings/src/css-modules.d.ts` 一条；#56 起 references 追加 `packages/daypaw/ui-tasks` 一行、include 追加 `packages/daypaw/ui-tasks/src/css-modules.d.ts` 一条；#60 起 references 追加 `packages/daypaw/ui-agents` 一行、include 追加 `packages/daypaw/ui-agents/src/css-modules.d.ts` 一条；#61 起 references 追加 `packages/daypaw/ui-brand` 一行（纯 token 层包，无 css-modules 条目） | 同上；项目引用无通配形式，只能逐包登记；css-modules 条目沿用 `packages/extensions/ui-cordis` 的聚合 include 先例 | 否（fork 私有包） | F（产品壳脚手架）/#55（收件箱骨架）/#59（设置单面页）/#56（任务对话）/#60（agent 目录）/#61（壳品牌主题） |
| `scripts/check-workspace-constraints.ts` | release-member 目录正则排除 `packages/daypaw/` 组与 `apps/daypaw-*`（负向前瞻）；另设 `publishableDaypawPackages`（cli/sdk）：豁免 private 要求、改验 `publishConfig.access: public` 与 fork 仓库 repository 字段，并豁免其 peerDependencies 的 workspace: 协议要求 | 上游 npm-public 化后 `packages/*/*`、`apps/*` 一律要求可发布，与 ADR 0001「@daypaw 独立 0.x private」相抵；ADR 0011 核准 cli/sdk 两包可发布、peer 指向上游 npm 发布 | 可提（上游无 daypaw；规则形状属上游发布流程） | C（走骨）/D（客户交付）/F（产品壳脚手架） |
| `packages/client/connection/src/client/fixture.ts`（连带 `tests/fixture.client.spec.ts` 断言随动：callId 配对、question 归属 fx-gamma、flipGammaRunning 显式开启、approvalHistory 折叠） | fx-alpha 日志追加两条 approval asked/decided 对与 turn-75 todo_write 样本（乘同一开放 turn 的已完成 step 0，避免后缀 turn/start 退役 standing plan 致上游 todo 面空置）；常驻审批带 callId 并与该开放 tool/call 配对；常驻 question 迁至 fx-gamma（避免 question 徽章在 run 孪生 session 上遮蔽 approval 徽章）；fx-gamma 的 5s running 翻转改为 `FixtureOptions.flipGammaRunning` 显式开启（环境翻转会与任何把 sessions 列表采进 golden 的车道竞态）；approvalHistory 折叠进 projectionValuesOf/projectionFramesOf。#90 起 durable/* 六端点应答、run/journal/定义三表与 rerun/start 串行迁出至 `apps/daypaw-web/tests/durable-rpc.ts` 的装饰器 transport（spec 同迁 `durable-rpc.spec.ts`），经 `__DSH_TRANSPORT__` 载体钩子装配（出口见 index.ts 行） | daypaw 组装快照车道的免 key 种子世界：任务板/审批待办/agent 目录 golden 依赖这些 rpc switch 之外的 fixture 世界内部种子，无法经公开面注入；durable 端点面已反转为 fork 自有缝 | 否（fork 私有种子的 fixture 调整） | #57（任务进度）/#58（审批待办）/#90（durable 迁装饰器） |
| `packages/client/connection/src/client/index.ts` | 公开再导出 `createFixtureConnectionRpc`（带一句用途注释） | #90：装饰器 transport 以 `ClientTransportHooks` 载体钩子装配 fixture 世界，公开面缺工厂出口；`./src/*` 子路径导入与 rewriteRelativeImportExtensions 的 emit 面不兼容（TS2877），唯一落点是 client 入口再导出 | 可提（测试壳以载体钩子装 fixture 的先例即 worker preview 的 postMessage 隧道） | #90（durable 迁装饰器） |
| `package.json`（root） | scripts 新增 `release:daypaw`（`tsx scripts/release/daypaw.ts`）、`check:ci:daypaw-hosted`（`tsx scripts/run-gates.ts ci-daypaw-hosted`，#72）、`dev:daypaw`（`bash scripts/dev-daypaw.sh`，#76）；`build:web` 扩展为同时构建 `@daypaw/web-frontend`；#56 起新增 `test:web:daypaw`（`npm run build && vitest run --config vitest.web.daypaw.config.ts`，含 `:built`/`:refresh` 变体，同 `test:web` 先例） | ADR 0011 发布工程面入口；fork 前端 dist 随全量 build 产出（spec 第 5 章 §4）；fork 组装 web 快照车道的脚本入口；源码态产品壳开发启动入口 | 否（fork 私有流程） | D（客户交付）/F（产品壳脚手架）/#56（任务对话） |
| `.gitignore` | 新增 `dist-daypaw/`（release 暂存与 tarball 输出）；新增 `apps/daypaw-web/dist/`（fork 前端构建产物）；#73 新增 `/daypaw/`（根锚定；工作区运行产物：ledger、wal/shm、CLI 播种与用户自写的 agents 目录文件——不锚定会误吞 `packages/daypaw/` 包树） | 同 `dist-exe/`、`apps/web/dist/` 先例 | 否（fork 私有产物） | D（客户交付）/F（产品壳脚手架） |
| `scripts/doc-budgets.manifest.json` | `AGENTS.md` 词预算 1900 → 1975 | 上游本体已顶满 1900，任何 fork layer 追加都超限；提上限而非持续压缩上游段落 | 否（fork 层增量无上游对应物） | C（走骨） |
| `scripts/translation-pairing.ts` | 翻译范围排除 fork 设计语料（docs/{adr,spec,agents,fork,reports,research}、CONTEXT.md） | fork 设计语料中文优先是图定决策（map #1），双语义务覆盖上游文档 | 否（fork 语料无上游对应物） | C（走骨） |
| `apps/web/tests/built-boot.expected.e2e.ts` | 常驻交互断言从「Waiting for answer」改为「Waiting for approval」 | #58 把常驻 question 迁至 fx-gamma 后，fx-alpha 的可操作等待是审批；上游烟雾钉的正是该 session 行的可操作等待镜像 | 可提（断言文案随 fixture 状态走，属上游测试自身的适应性修改） | #58（审批待办） |
| `apps/web/tests/todo-row.expected.e2e.ts` | 文件头注释的并行计划 turn 编号 74→75 | #58 turn-75 todo_write 样本落位后的适应性修改——上游 todo-row 烟雾的头注钉的正是该样本的供数状态（turn 与双 `in_progress` 项） | 可提（注释随 fixture 状态走，属上游测试自身的适应性修改） | #58（审批待办） |
| `scripts/client-tsconfig.spec.ts` | `clientCssDeclarations()` 的 clientGroups 枚举新增 `'daypaw'` | client 聚合 css-modules 契约测试的 group 枚举须覆盖 `packages/daypaw/*/src/css-modules.d.ts`（ui-inbox 自 #55 起在 include 里，枚举漏了该组） | 可提（枚举随 group 增长是上游机制；daypaw 组属 fork） | #59（设置单面页） |
| `scripts/gen-cordis-catalog.ts` | `ctx.durable` 入 SERVICE_WALK_EXEMPTIONS；EngineDefinition/EngineRunHandle/EngineRunOptions 入 TYPE_LINK_EXEMPTIONS（#53 起 steer wire 输入的 `Json` 类型同入） | fork 服务的文档之家是包 README（上游子系统目录外），签名类型链接随之豁免 | 可提（豁免模式本身是上游机制；具体条目属 fork） | C（走骨）/#53（steer 通道） |
| `package.json`（root） | devDependencies 新增 `zod@^4.4.3` | doc-typecheck 从根级临时项目编译文档代码块，裸 `zod` 导入只能经根 node_modules 解析（pnpm 不提升到根）；daypaw spec/README 的正典类型与示例必须 import zod | 否（上游文档块未导入 zod，无对应需求） | E（defineAgent 编译面） |
| ~~`packages/boot/app-boot/src/profile.ts`（#64）~~ | **已消解（2026-08-28 sync）**：上游重写 heal 面为 `ProfileModuleFallbackOptions` + module-proxy 机制后，daypaw/cli 只调用上游公开的 `healProfilesModuleFallback({installAnchor, home})`，不再需要导出 `ensureSymlink`——fork 副本已随 #62 删除，无上游文件改动 | — | ~~#64~~（2026-08-28 sync 消解） |
| `packages/client/ui-theme/src/theme-settings.ts`（连带 `tests/{theme,boot-theme,settings-store,host}.client.spec.ts` 默认断言、README(.zh) 默认描述两句、`apps/web/tests/expected/settings-chrome/dialog.expected.md` 外观行 pressed 态 golden 刷新） | `DEFAULT_PREFERENCE` 从 `'system'` 改为 `'light'` | #61：spec 05 §7 裁决「亮主题默认」；settings namespace 归属注册不可重叠、插件无 config 钮、种子用户 settings.yaml 会污染与 dsh 共享的文档——ADR 0001 §4 三问皆无解，唯一落点是常量本身。影响面：上游 dsh web 壳的默认偏好同步变 light（TUI 无此插件）；偏好切换面（含跟随系统）不变 | 否（daypaw 产品决策；上游 dev 工具默认 system 是其自身取舍） | #61（壳品牌主题） |
| `scripts/verify-application-entrypoints.ts` | `MANIFEST_BIN_ALLOWLIST` 增 `packages/daypaw/cli`（`daypaw` → `bin.mjs`）；`EXECUTABLE_SOURCE_ALLOWLIST` 增 `packages/daypaw/cli/bin.mjs` | ADR 0011：daypaw CLI 是自含交付（bin 直起、不经 dsh launcher），登记为合法例外 | 否（fork 交付形态） | 2026-08-28 sync |
| `scripts/translation-pairing.manifest.json` | `excluded` 增 fork 单语设计语料（`docs/adr/`、`docs/spec/`、`docs/agents/`、`docs/fork/`、`docs/reports/`、`docs/research/`、`CONTEXT.md`） | fork 设计语料中文优先（map #1 定案），无英文 twin；上游新配对规则（2026-08-28 收紧）要求 zh 侧链接带 `.zh`，语料外目标保持 authored 路径 | 否（fork 语料无上游对应物） | 2026-08-28 sync |
| `scripts/run-gates.ts`（连带 `scripts/run-gates.spec.ts` 枚举与 root `package.json` 脚本行） | 新增加性聚合模式 `ci-daypaw-hosted`：上游 ciPrimaryGates 的全部确定性 gate + web 快照道，减两条全量测试道（coverage、录制会话快照） | #72：hosted 4-vCPU 上这两条道的时序敏感进程测试测的是宿主而非代码（上游在 16 核企业 runner 上跑、其自身 hosted 串行参照本就禁用）；fork main 车道以「必需确定性门 + advisory 时敏道」双作业呈现 | 否（fork 车道形态；gate 复用上游构件） | #72（main 车道收敛） |
| `packages/terminal/terminal-bash/tests/local.spec.ts` | pwsh 真壳用例的 harness 时序 `idleSilenceMs`/`handoffGraceMs` 300→2000 | #72：hosted 4-vCPU CI 车道上冷 pwsh 的 profile 引导静默超过 300ms，`stdin_read` 证据需要更宽窗口，否则 `inferred_idle` 先胜；harness 的 timing 形参即为此设计 | 可提（时序值随宿主而调，机制属上游测试自身） | #72（main 车道收敛） |
| `vitest.config.ts` | coverage exclude 新增一条 glob `packages/daypaw/ui-*/src/**` | ADR 0007 §1 裁决：fork 的 daypaw ui-* 家族沿用上游 client UI 的 GUI 债豁免姿态（jsdom 组件测试 + 组装 web 车道，不受 per-file 门约束）；host 侧包（engine 等）仍走 100% 门 | 可提（豁免模式本身是上游机制；具体 glob 属 fork 家族） | #71（覆盖率门修复） |
| `scripts/release/families.ts` | `DshFamily.members()` 跳过 fork 自营目录（`packages/daypaw/`、`packages/examples/daypaw-skeleton/`、`apps/daypaw-web/`） | @daypaw/* 包（ADR 0011 客户交付形态）由独立 daypaw release family 发布，不混入 dsh family；patterns 保持上游原样减少 sync 冲突 | 否（fork 包命名架构） | 2026-08-28 sync |
| `packages/api/session-controller/src/client/sessions/manager.ts`（连带 `tests/manager.client.spec.ts` 两例、`src/client/sessions/service.ts` 的 `list` 字段 JSDoc、README(.zh) removal 帧重拉段） | `handleSessionRemoved` 在应用 removal 帧后追加一次单飞 `refreshList()` 对账重拉 | #94：活 Session 的 dispose 不是 durable 删除——引擎任务 run 终态时 SDK body 的 handle.dispose 发出孪生的 `api-session/removed`，客户端只应用不移除重拉会把仍持久化的孪生挤出列表直到重连；重拉后持久化者复活、真删除者保持缺席。影响面覆盖上游 dsh web 客户端（上游同样受益：任何「dispose 但持久化」的 session 不再凭空消失） | 可提（对账语义属上游列表机制本身） | #94（运行中对话可达） |
| `packages/client/modules/src/client/manifest.ts` + `src/index.ts`（连带 `tests/{loader,node-half}.client.spec.ts`、`README(.zh)`、`docs/subsystems/client-modules.md(.zh)`） | 浏览器 boot wire 增行 config 通道：`WebBootEntry`/`BootPluginRow` 增可选 `config` 字段并在解析面透传；宿主半侧按包清单新声明 `dsh.client.config: true` 决定是否携带该行 cordis config（默认不携带——双面包行可继续持有仅宿主可用的 `!!js` 表达式），携带前校验 JSON 可序列化（函数/符号/undefined/bigint 组合期大声拒绝） | #105：浏览器插件此前完全没有 config 通道（roster 行 config 在 boot 处被静默丢弃——上游潜在缺口）；单壳分层需要 ui-trajectory 把注册目标改指 fork 检查器环，而目标只能经行 config 下发。默认行为逐字节不变（无声明即无携带） | 可提（通道修的是上游自身的潜在缺口：ui-conversation 等浏览器行的 Config 字段今天实际从不生效；`dsh.client.config` 声明制保持上游组合零变化） | #105（trajectory 检查器 tab） |
| `packages/client/web/src/boot.ts`（连带 `tests/boot.client.spec.ts`） | 插件激活循环把 wire 行 config 传入 `loader.create({ name, config })`，cordis 侧照常按插件 Config schema 校验后交给 apply | 同上：通道的浏览器端落点 | 可提（同上） | #105（trajectory 检查器 tab） |
| `packages/client/ui-trajectory/src/client/index.ts` + `package.json`（连带 `tests/plugin.client.spec.ts`、`README(.zh)`） | 客户端入口新增导出 `Config`（`viewSlot` 字符串，默认 `'conversation.view'`）并在 `dsh.client` 清单声明 `config: true`；apply 把 view-ring 注册目标改为该配置值 | #105：fork 对话座持有自声明的检查器环，trajectory 账本原样（verbatim 事件数据）注册进去；默认值保持上游对话壳行为 | 可提（「在非上游壳的环里挂载账本」是通用宿主能力；默认不变） | #105（trajectory 检查器 tab） |
